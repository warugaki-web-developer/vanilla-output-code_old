{
  "name": "vanilla-output-code",
  "version": "1.0.0",
  "engines": {
    "node": "18.12.1",
    "npm": "8.19.2"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format:check": "prettier --check --ignore-path .gitignore './**/*.{js,ts,json,css}'",
    "format:fix": "prettier --write --ignore-path .gitignore './**/*.{js,ts,json,css}'"
  },
  "author": "warugaki",
  "license": "MIT",
  "devDependencies": {
    "prettier": "^2.8.1"
  }
}
